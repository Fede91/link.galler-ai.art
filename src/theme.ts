import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Tsukimi Rounded', sans-serif`,
    body: `'Satoshi', sans-serif`,
  },
});

export default theme;
